export function quizScore(num) {
    if (num > 2) {
        return 'Great Job!';
        let returnColor = style
    }
    return 'You should study a bit more!';

}

export function resultsAsPercentage(results) {
    return ((results / 3) * 100).toFixed(0) + '%';
}

// quizScore.classList.add('pink, green'); 